import type { StickFigureConfig } from '../types';

/**
 * Hook for managing stick figure animation state.
 * Stub — returns an empty array of figures.
 */
export function useStickFigures(): StickFigureConfig[] {
  return [];
}
